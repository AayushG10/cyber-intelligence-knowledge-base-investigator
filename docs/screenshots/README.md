# Screenshots

Drop PNGs here with these exact names and the main README will pick them up automatically:

- `landing.png` — the marketing landing page (`http://localhost:5173`, hero section)
- `dashboard-overview.png` — command centre, Overview tab, graph + ring detail
- `dashboard-map.png` — Map tab, cross-jurisdiction hotspots
- `dashboard-agent.png` — Agent tab, a live tool-calling answer
- `dashboard-evidence.png` — Evidence tab, a sealed intelligence package
- `entity-drawer.png` — the entity dossier drawer with the risk breakdown

Easiest way to capture: run the app (`npm run dev` + backend on :8010), open each screen,
and use your OS screenshot tool (macOS: `Cmd+Shift+4`).
